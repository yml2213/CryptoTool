module.exports = {
    content: ['./src/**/*.{html,js,vue}'],
    darkMode: 'class',
    theme: {
        extend: {
            colors: {
                accent: 'var(--accent-color)'
            }
        }
    },
    plugins: []
} 